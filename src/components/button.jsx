import { Button } from "@mui/material";

const CustomButton = () => {
  return (
    <>
      <Button variant="Contained">
        Sign In
      </Button>
    </>
  );
};

export default CustomButton;
